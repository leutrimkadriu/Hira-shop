{% assign product = section.settings.product %}
{% assign product_image = product.featured_image %}
<div class="product-image">
    <h1>{{ product.title }}</h1>
    <img src="{{ product_image | image_url: width: 400 }}" alt="{{ product.title }}" width="400" height="400">
   
    <p>{{ product.description }}</p>
    <p>Price:{{ product.price |  money }}</p>
    <p>Compare at price :{{ product.compare_at_price | money }}</p>
    <p>{{ product.compare_at_price_min |  money}}</p>
    <p>{{ product.compare_at_price_max | money}}</p>
</div>
>>>>>>> ca9840a (..)
{% schema  %} 
{
    "name": "Test 3",
    "settings": [
        {
            "type": "text",
            "id": "test",
            "label": "Test"
        },
        {
            "type": "product",
            "id": "product",
            "label": "Product"
        }
    ],
    "presets": [
        {
            "name": "Test 3"
        }
    ]
}
{% endschema %}